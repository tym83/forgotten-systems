build/
__pycache__/
*.pyc
tests/*.bin
tests/*.chk
# Собираемые артефакты — правила в Makefile (цели tools, web)
ext/norebo/norebo.bin
ext/norebo/norebo.orig.bin
ext/refemu/refboot
ext/refemu/reftrace
web/oberon.dsk.gz

# ⚠ risc5.js, risc5.wasm, oberon.dsk и prom_sd.mem НЕ игнорируются: GitHub Pages
# раздаёт статику как есть, а собрать wasm на раскладке нечем — нужны verilator и
# emsdk. Пересобираются целью `web`, изменения видны в diff'е. Раньше они
# игнорировались здесь и жили руками скопированными в site/oberon/ — копия
# устарела молча.
